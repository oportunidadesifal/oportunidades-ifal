<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Oportunista - Cadastro Oportunidade</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" type="text/css" href="/css/cadastraOportunidade.css">
</head>
<body>
    <div class="row main-row m-0">
        {% include 'layouts/_sidebar.html' %}
        <div class="col-md-9 col-lg-10 content-div">
            {% if success != false %}
                <div class="alert alert-success alert-dismissible mt-5">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    {{ success }}
                </div>
            {% endif %}

            {% if error != false %}
                <div class="alert alert-danger alert-dismissible mt-5">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    {{ error }}
                </div>
            {% endif %}

            <div class="row justify-content-center mt-5">
                <p class="teacherName whiteText col-12 text-center mb-0 mt-5">{{ user.profile.name }},</p>
                <p class="whiteText">que tipo de oportunidade deseja cadastrar?</p>
            </div>
            <div class="row justify-content-center buttons mt-3">
                <div class="col-xs-11 col-md-4">
                    <div class="mt-3">
                        <a href="{{ path_for('researchForm') }}" class="btn btn-md btn-light btn-block">Pesquisa</a>
                    </div>
                    <div class="mt-3">
                        <a href="{{ path_for('eventForm') }}" class="btn btn-md btn-light btn-block">Evento</a>
                    </div>
                    <div class="mt-3">
                        <a href="{{ path_for('monitoringForm') }}" class="btn btn-md btn-light btn-block">Monitoria</a>
                    </div>
                    <div class="mt-3">
                        <a href="{{ path_for('jobForm') }}" class="btn btn-md btn-light btn-block">Estágio ou Emprego</a>
                    </div>
                    <p class="whiteText col-12 text-center mt-4 mb-0">Precisa avisar algo a seus alunos?</p>
                    <p class="whiteText urgentNews col-12 text-center">Envie uma notícia urgente</p>
                    <a href="{{ path_for('newsForm') }}" class="btn btn-md btn-success btn-block oppGreenButton">Noticia Urgente</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js" integrity="sha384-smHYKdLADwkXOn1EmN1qk/HfnUcbVRZyYmZ4qpPea6sjB/pTJ0euyQp0Mk8ck+5T" crossorigin="anonymous"></script>
</body>
</html>